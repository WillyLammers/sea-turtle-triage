services:
  - type: web
    name: sea-turtle-triage
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start --workspace=server
    envVars:
      - key: NODE_ENV
        value: production
